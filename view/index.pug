head
  meta(charset='utf-8')
  meta(name='viewport' content='width=device-width, initial-scale=1')
  
  link(rel="icon" href="img/favicon.ico" type="image/x-icon")
  
  link(href='https://fonts.googleapis.com/icon?family=Material+Icons' rel='stylesheet')
  <link href="https://fonts.googleapis.com/css2?family=Caveat+Brush&family=Cinzel:wght@400;500&display=swap" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Noto+Serif+KR:wght@300&display=swap" rel="stylesheet">
  script(src="https://code.jquery.com/jquery-2.1.3.min.js")
    
  link(rel='stylesheet' href='/order.css')
  meta(name="title" content="Baking Bunny")
  meta(name="description" content="We are making fresh & delicious dessert")

  meta(property="og:title" content="Baking Bunny")
  meta(property="og:type" content="website")
  meta(property="og:url" content="https://baking-bunny.herokuapp.com/")
  meta(property="og:image" itemprop="image" content="https://baking-bunny.herokuapp.com/img/custom.jpg")
  meta(property="og:description" content="We are making fresh & delicious dessert")
  meta(name="theme-color" content="#f8e3de")

  meta(name="twitter:card" content="summary_large_image")

  title Baking Bunny

body

  .container
    section.welcome
      .text
        h1 Baking Bunny
        P Baking Bunny is specialized in 
        p flower cakes, custom designed cakes,
        p and dacquoise in birthdays, showers, 
        p and every other occaion in Calgary, Alberta        
        br
        br
        a.welcome__buton(href='/order/eng')
          button(type='button') English
        a.welcome__buton(href='/order/kor')
          button(id="kor" type='button') 한국어
        button.inquiry(type='button') Inquiry

      a(href="https://www.instagram.com/baking.bunny_yyc/")
        img.bunny_icon(src="/img/logo_circle.png")
            
    .inquiry_modal
      .inquiry_modal__content
        section.loading
          .loading_text
            p Now sending the E-mail...
        
        section.sent
          .text_content
          button.close_two(type='button') Close

        form.contact_form(method='POST' action='')
          input.name(type='text' name='name' placeholder='Name' required)
          input.email(type='text' name='email' placeholder='E-mail' required)
          .date
            label(for="date") Needed Date
            input.date_need(type='date' name='date')             
          textarea.text(placeholder='Message' name='Message' rows='4' cols='50' required)
          .button
            button.send(type='submit') Send 
            button.close_one(type='button') Close
        
        
        
                
script(type='text/javascript' src='/js/inquiry.js')
